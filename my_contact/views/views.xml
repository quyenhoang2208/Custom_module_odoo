<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="my_contact.list">
      <field name="name">my_contact list</field>
      <field name="model">my_contact.my_contact</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="my_contact.action_window">
      <field name="name">my_contact window</field>
      <field name="res_model">my_contact.my_contact</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="my_contact.action_server">
      <field name="name">my_contact server</field>
      <field name="model_id" ref="model_my_contact_my_contact"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="my_contact" id="my_contact.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="my_contact.menu_1" parent="my_contact.menu_root"/>
    <menuitem name="Menu 2" id="my_contact.menu_2" parent="my_contact.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="my_contact.menu_1_list" parent="my_contact.menu_1"
              action="my_contact.action_window"/>
    <menuitem name="Server to list" id="my_contact" parent="my_contact.menu_2"
              action="my_contact.action_server"/>
    -->
  </data>
</odoo>